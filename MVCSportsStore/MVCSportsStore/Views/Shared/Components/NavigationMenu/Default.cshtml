@model IEnumerable<string>

@foreach (var category in Model)
{
    <a class="btn btn-block btn-outline-secondary m-1" asp-controller="Home" asp-action="Index" asp-route-id="1">
        @category
    </a>
    <br />
}